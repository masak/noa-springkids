import React, { Fragment } from 'react';
import './App.css';
import GameBoard from './components/GameBoard';

const App = (props) => (
  <Fragment>
    <GameBoard />
  </Fragment>
);

export default App;
